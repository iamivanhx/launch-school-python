# Can you write some code to change the value 'bye' in the following tuple to 'goodbye'?
stuff = ('hello', 'world', 'bye', 'now')
stuff = ('hello', 'world', 'goodbye', 'now')

# Tuples are immutable. The only option is to reassign the tuple.