# Without running the following code, what do you think it will do?

def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello') # It raises an exception as the function is invoked with only 1 argument and two are expected, as the function is defined with two parameters.